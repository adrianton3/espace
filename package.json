{
  "name": "espace",
  "version": "0.0.0",
  "devDependencies": {
    "uglify-es": "^3.1.10"
  },
  "scripts": {
    "uglify": "uglifyjs src/**/*.js > build/espace.min.unwrap.js",
    "wrap": "cat tools/header.js build/espace.min.unwrap.js tools/footer.js > build/espace.min.js",
    "build": "npm run uglify && npm run wrap && rm build/espace.min.unwrap.js"
  }
}
